export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Neo4j + Browser Bulk Uploader",
  description: "Upload data to Neo4j using a browser extension.",
  links: {
    github: "https://github.com/heroui-inc/heroui",
  },
};
